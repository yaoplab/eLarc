---
name: toolkit-reference
description: Catalogue des widgets phibuilder — 27 widgets M3 (bouton, card, combo, table, dialog, snackbar...) + architecture phi/theme/style.
category: design
trigger: widget, phibuilder, M3, bouton, card, combo, table, dialog
---

# Toolkit Reference — Widgets phibuilder

## Widgets disponibles (27)

`M3Button`, `M3Card`, `M3ComboBox`, `M3TextField`, `M3Label`, `M3TableWidget`, `M3Dialog`, `M3Snackbar`, `M3BottomSheet`, `M3Navigation`, `M3SidebarNav`, `M3ChipBar`, `M3DateEdit`, `M3TimeEdit`, `M3TextEdit`, `M3GroupBox`, `M3Menu`, `M3Tab`, `M3Frame`, `M3ScrollArea`, `M3Splitter`, `M3StackedWidget`, `M3HeaderView`, `M3ProfileButton`, `M3DialogButtonBox`, `M3ProgressBar`, `M3ListWidget`

## Imports

```python
from phibuilder.widgets.button import M3Button, ButtonVariant
from phibuilder.widgets.table import M3TableWidget
from phibuilder.widgets.scrollarea import M3ScrollArea
```
